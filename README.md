# Clash Resources

由 CircleCI 自动发布的 Clash / Mihomo 规则资源。

- `current/common.ini`：subconverter 自定义模板
- `current/rules.module`：OpenClash 远程覆写模块
- `current/rules/*.mrs`：按分类编译的 MRS 规则集

`main` 分支与部署凭据由维护者一次性初始化；CI 只更新 `current/`。
