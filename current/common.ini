[custom]
; 在线转换只生成节点与一个手动选择组。规则由 CI 生成的远程覆写模块接管。
; 这条 FINAL 仅保证转换器输出非空，覆写模块会完整替换 rules。
ruleset=🚀 节点选择,[]FINAL

; 不做自动测速、地区分组或业务策略组。分类信息保留在 RULE-SET provider 名称中。
custom_proxy_group=🚀 节点选择`select`.*`[]DIRECT

enable_rule_generator=false
overwrite_original_rules=false
skip_failed_links=true
